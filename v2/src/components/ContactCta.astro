---
import { useTranslations, type Lang } from '../i18n/utils';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
---

<section id="contact" class="border-t border-[var(--color-hairline)] py-12 md:py-16">
  <div class="mx-auto max-w-6xl px-6 md:px-10">
    <div data-reveal class="max-w-xl">
      <p class="text-xs tracking-[0.16em] text-[var(--color-brass)] uppercase">
        {t('contact.eyebrow')}
      </p>
      <h2 class="mt-2 font-display text-2xl font-semibold leading-[1.2] text-[var(--color-paper)] sm:text-3xl">
        {t('contact.heading')}
      </h2>
      <p class="mt-3 text-sm leading-relaxed text-[var(--color-paper-muted)] md:text-base">
        {t('contact.body')}
      </p>
    </div>

    <div data-reveal class="mt-6 flex flex-wrap items-center gap-3">
      <a
        href="mailto:redyamusic@outlook.com"
        class="rounded-full bg-[var(--color-paper)] px-6 py-3 text-sm font-medium text-[var(--color-ink)] transition-opacity duration-200 hover:opacity-90"
      >
        {t('contact.ctaPrimary')}
      </a>
      <a
        href="https://wa.me/31657337248"
        target="_blank"
        rel="noopener noreferrer"
        class="rounded-full border border-[var(--color-hairline-strong)] px-6 py-3 text-sm text-[var(--color-paper)] transition-colors duration-200 hover:border-[var(--color-brass)]"
      >
        WhatsApp
      </a>
      <a
        href="https://t.me/merxio_manager"
        target="_blank"
        rel="noopener noreferrer"
        class="rounded-full border border-[var(--color-hairline-strong)] px-6 py-3 text-sm text-[var(--color-paper)] transition-colors duration-200 hover:border-[var(--color-brass)]"
      >
        Telegram
      </a>
    </div>
  </div>
</section>
